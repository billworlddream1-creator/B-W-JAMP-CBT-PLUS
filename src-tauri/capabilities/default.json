{
  "$schema": "../node_modules/@tauri-apps/cli/schema.json",
  "identifier": "default",
  "description": "Capability for default window access",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
